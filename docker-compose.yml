version: '2'

services:
  nginx:
    image: adms_account_nginx:v1.0.0
    depends_on:
      - php-fpm
    ports:
      - 8080:8080
    #volumes:
    #  - ./nginx-vhost/myapp.conf:/bitnami/nginx/conf/vhosts/myapp.conf
    #  - .:/app

  php-fpm:
    image: adms_account_phpfpm:v1.0.0
    #volumes:
    #  - .:/app
